## Customize the test machine
machine:
  pre:
    - pwd
    - ls
    - cd aws-automation-thing && ./djangosetup.sh

## Customize deployment commands
deployment:
  staging:
    branch: dev
    commands:
      - ls
      - fab deploy
      - cd ~/.ssh
      - ls
